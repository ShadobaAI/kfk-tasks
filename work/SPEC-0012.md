---
task: SPEC-0012
status: active
affected_repositories: [kafka-tools, kfk-tasks]
committed_revisions:
  kafka-tools: 45e76ed7d8a29c0c0f4161b78c7a0412d00fada9
  kfk-tasks: 69ffa77706af89aaaa3cf1d713cba71fc8199581
---

# Handoff: SPEC-0012

## Completed work

Реализация находится в незакоммиченном working tree поверх указанных revisions.
Эти commits сами по себе не содержат текущую реализацию; handoff не является
доказательством committed team continuity.

- Doctor подключает managed code-index health через bounded stdio session,
  без daemon bootstrap/restart. Live проверка всех семи aliases проходила в более
  ранней сессии; в текущем продолжении она не повторялась по указанию пользователя.
- Installed-profile smoke использует последовательный MCP handshake из foreign cwd.
- OpenViking использует latest-stable policy по official PyPI metadata и проверяет
  digest выбранного wheel; readiness gate требует рабочие embedding/vector DB/AGFS
  checks, а не только top-level ready.
- Fresh read-only review doctor выполнен, два найденных дефекта исправлены.
- Bounded v8std check и fixtures подтверждают разделение recommended std436
  exception и mandatory corporate justification.
- Реализован и изолированно проверен eager Git hook manager для checkout,
  merge/pull и rewrite/rebase. Он сохраняет foreign hooks, их exit status,
  делает global preflight/lock и оставляет pre-use reconcile correctness boundary.
- `hookRepositories` фиксирует все девять Kafka-owned repositories; upstream
  `tests/unit/yaxunit` явно исключён из установки hooks.
- Local two-checkout E2E подтвердил одинаковый committed inventory, исключение
  dirty/untracked content и фактические post-merge/post-checkout/post-rewrite triggers.
- Добавлен latest-stable OpenViking bootstrap по official PyPI metadata с SHA-256,
  staging venv, versioned runtime directories и запретом workspace placement. Полный `install.cmd`
  автоматически устанавливает/проверяет runtime, при отсутствии user config вызывает
  официальный interactive provider init, запускает server, делает initial sync,
  MCP smoke и ставит hooks. Каждый запуск проверяет update; предыдущие runtime сохраняются.

## Changed contracts and accepted decisions

См. [SDD](../sdd/spec-0012-codex-context-quality-and-cost.md),
[ADR-0005](../adr/adr-0005-git-backed-disposable-semantic-read-model.md) и
owning `kafka-tools/ai/README.md`. TeamAI отменён; не восстанавливать integration,
compatibility mode или fallback. Production cutover запрещён до всех gates.
EDT исключён пользователем из текущего workstream. Installed EDT skill differences
не перезаписывать без проверки. Посторонний SPEC-0013 сохранять.

## Unresolved questions

- Последняя выполненная live OpenViking readiness вернула `fetch failed`; в текущем
  продолжении backend не проверялся и не устанавливался. Live sync/retrieval,
  provider acceptance и runtime update acceptance остаются незакрытыми.
- EDT readiness/detector/compliance остаются отдельным acceptance blocker.
- Полная v8std completeness, остальные mixed-strength rules, BSL LS и actual
  Codex discovery не подтверждены.
- Forced teardown descendants зависшего MCP launcher не доказан.

## Next actions

1. После нового запроса пользователя и recovery проверить OpenViking readiness;
   provider/model/credentials не угадывать и не читать personal config.
2. Проверить latest-stable runtime update, safe server replacement и required rebuild;
   update check и hook manager уже встроены в installer, но live provider/runtime
   acceptance на целевой машине ещё не выполнена.
3. Проверить остальные mixed policy strengths по exact v8std evidence.
4. Выполнить live runtime E2E для actual latest update и отдельно проверить
   handoff/orchestration; EDT-dependent checks сохранять отдельно.
5. Снять actual Codex schemas/usage на фиксированном benchmark suite;
   byte inventory не обозначать tokens или rendered Codex schemas.
6. Подготовленные границы commits: (a) bounded stdio client; (b) policy/OpenViking
   implementation, их focused fixtures и owning docs; (c) doctor и installed
   handshake integration; (d) tasks SDD/ADR/handoff. Doctor зависит от (a)/(b).
   Перед staging сверить весь существующий diff; SPEC-0013 не включать.

## Verification and gaps

После перехода на latest-stable прошли `test-installation.ps1`,
`test-openviking-manifest.mjs`, `test-openviking-sync.mjs`,
`test-openviking-mcp.mjs`, `test-openviking-hooks.mjs`,
`test-openviking-checkouts.mjs`, `test-doctor.mjs`, Node/PowerShell syntax checks и
tools/tasks `git diff --check`. Checkout E2E использует temporary local clones;
sync/MCP/runtime installation остаются mock/static там, где нужен live backend.
Official PyPI metadata прочитана без установки package. EDT-MCP и code-index по
указанию пользователя в текущем продолжении не проверялись. Production installer не
запускался, commits не создавались. SPEC-0012 остаётся `active`.
