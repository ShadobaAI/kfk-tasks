[mcp_servers.memory-bank]
command = "python"
args = ["-m", "memory_bank_mcp.server"]
cwd = "${KAFKA_PROJECTS_ROOT}/tasks"

[mcp_servers.memory-bank.env]
KAFKA_PROJECTS_ROOT = "${KAFKA_PROJECTS_ROOT}"

